<h1 class="d-flex justify-content-center"><%= t('.header') %></h1>
<table class="d-flex justify-content-center table table-striped">
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions') %></th>
    <th colspan="3"><%= t('.action') %></th>
  </tr>
  <%= render @tests %>
</table>

<p><%= link_to t('.new_test'), new_admin_test_path %></p>
