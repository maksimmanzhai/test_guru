<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('.header') %></span>

  <div class="nav-admin user">
    <%= t('admin.nav.greeting', email: current_user.email) %></b> | 
    <%= link_to t('admin.nav.panel'), admin_tests_path %></b> | 
    <%= link_to t('admin.nav.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>
